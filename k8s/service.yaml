apiVersion: v1
kind: Service
metadata:
  name: palindrome-service
spec:
  selector:
    app: palindrome
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9091
  type: ClusterIP
